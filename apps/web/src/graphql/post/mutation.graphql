mutation createPost($input: CreatePostInput!) {
  createPost(input: $input) {
    ...Post
    author {
      ...User
    }
  }
}

mutation vote($input: VoteInput!) {
  vote(input: $input) {
    ...Post
    author {
      ...User
    }
  }
}
